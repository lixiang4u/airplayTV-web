/**
 * @file 其他命名方式变下划线，可传入正则（默认小驼峰匹配）
 */
/**
 * 其他命名方式变下划线，可传入正则（默认小驼峰匹配）
 *
 * @param str 待转换字符串
 * @param reg 匹配正则
 *
 * @returns 转换之后的值
 */
export default function underScoreCase(str: any, reg?: RegExp): string;
