/**
 * @file 判断版本
 */
/**
 * 判断给定版本是否符合条件
 *
 * @param version 待检查版本
 * @param checkVersion 版本基准
 * @param equal 是否判等
 *
 * @returns 是否符合条件
 */
export default function checkVersion(version: string, checkVersion: string, equal?: boolean): boolean;
