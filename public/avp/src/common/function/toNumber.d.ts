/**
 * @file 强转为 number
 */
/**
 * 强转为 number
 *
 * @param target 待转换值
 * @param defaultValue 默认值
 *
 * @returns 转换之后的值
 */
export default function toNumber(target: any, defaultValue?: number): number;
